{{- $values := .Values -}}
{{- $release := .Release -}}
{{- $chart := .Chart -}}
{{- range $values.apps}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $release.Name }}-{{ .name }}-{{ $values.sha }}-deployment
  namespace: {{ $release.Namespace }}
  labels:
    chart: "{{ $chart.Name }}-{{ $chart.Version | replace "+" "-" }}"
spec:
  selector:
    matchLabels:
      app: {{ $release.Name }}
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ $release.Name }}-deployment
    spec:
      containers:
        - name: {{ .name }}
          image: {{ .image }}:{{ .tag }}
          ports:
            - containerPort: {{ .port }}
          env:
          {{- range .env_var}}
            - name: {{ .name }}
              value: {{ .value | quote }}
          {{- end}}
          {{- range .secrets}}
            - name: {{ .name }}
              valueFrom:
                secretKeyRef:
                  name: {{ .secret }}
                  key: {{ .key }}
          {{- end}}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .port | int }}
          readinessProbe:
            httpGet:
              path: /
              port: {{ .port | int }}
{{- end}}
